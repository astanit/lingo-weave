# OpenRouter API key (required for translation). Get one at https://openrouter.ai
OPENROUTER_API_KEY=

# For local docker-compose: backend URL the browser will call (e.g. http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Comma-separated origins for CORS (backend). Include your frontend URL.
CORS_ORIGINS=http://localhost:3000
